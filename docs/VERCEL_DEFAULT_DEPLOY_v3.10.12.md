# StayHub v3.10.12 – Vercel Default Deploy

Použi default Vercel nastavenia:

- Framework Preset: Vite
- Build Command: prázdne alebo `npm run build`
- Output Directory: prázdne alebo `dist`
- Install Command: prázdne

Neuploadovať do GitHubu: `node_modules`, `dist`, `.vercel`.

Tento balík neobsahuje package-lock.json, aby si Vercel vytvoril čistú npm inštaláciu.

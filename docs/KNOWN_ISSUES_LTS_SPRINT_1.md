# Known Issues – LTS Sprint 1

- The application still contains a large legacy `App.jsx`; Sprint 1 adds real module files but does not fully migrate all screens yet.
- AI OCR requires the server-side OCR endpoint and keys to be configured before production use.
- End-to-end workflow must still be regression-tested on live Supabase data.
